<!DOCTYPE html>
<html>

<head>
    <title> {{title}} </title>
    <style type="text/css">
        table {
            border-collapse: collapse;
            border: 1px solid black;
            width: 50%;
            background-color: #EEEEEE;
            font-size: 20px;
        }

        td {
            border: 1px solid black;
            padding: 13px;
        }
        tr {

        }
        .cell{
            width: 20%;
            display: inline-block;
            border: 1px solid grey;
        }
    </style>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <!--<script>
        function fillTab(song){
            console.log("starting to fill");
            var i = 0;
            song.forEach(function(element) {
                $(i).append("<tr>" + element + "</tr>");
                console.log("added element: " + element);
                i++;
                if (i > 3) {
                    i = 0;
                }
            });
        }
    </script>-->
</head>

<body>
    <h1>{{title}}</h1>
    <div>
        <h1>{{song.id}}  title: {{song.title}}</h1>
        <p>composer: {{song.composer}}</p>
        <p>key: {{song.key}}</p>
        <p>bars: {{song.bars}}</p>
        <table>

                {{#each song.individualBars}}
                <td class=cell>{{this}}</td>
                {{/each}}

        </table>
    </div>
</body>

</html>
